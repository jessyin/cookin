specVersion: "v1beta"
serviceId: "cookin-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cookin-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
